# @anthril/vguard-license-check

Checks dep licenses against a configurable allow/deny list when the agent edits `package.json`. Wraps `license-checker` (shelled via `npx`). Ships two presets: `license-permissive` and `license-copyleft-safe`.
